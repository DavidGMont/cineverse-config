server:
  compression:
    enabled: true
    mime-types: application/json
    min-response-size: 1024
  port: 0

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info